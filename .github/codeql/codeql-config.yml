name: "86Box CodeQL config"

queries:
  - uses: security-extended
#  - uses: security-and-quality

paths-ignore:
  - src/cdrom/libchdr/deps/
  - src/cdrom/libchdr/dr_libs/

query-filters:
  - exclude:
      id: cpp/path-injection
